<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.app.repository.MembersRepository">
    <select id="selectAll" resultType="com.graduation.app.record.MembersRecord">
        SELECT id, member_name, birthday, favorite_food, hobby, bumon_name FROM members
        <!-- FROM スキーマ名/テーブル名 -->
    </select>
</mapper>